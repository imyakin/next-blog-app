/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['i.ibb.co', 'image.pngaaa.com', 'testing-library.com', 'react-hook-form.com', 'upload.wikimedia.org'],
  },
}

module.exports = nextConfig
