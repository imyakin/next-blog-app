/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['media.licdn.com', 'image.pngaaa.com', 'testing-library.com', 'react-hook-form.com'],
  },
}

module.exports = nextConfig
